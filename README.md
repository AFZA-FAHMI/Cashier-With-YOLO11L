# Cashier-With-YOLO11L
A Point of Sale (POS) system combining barcode scanning and AI object detection. It employs a dual-verification logic for theft prevention. The system utilizes a race condition mechanism where barcode input is prioritized, requiring only a 40% AI validation threshold to accelerate the scanning process.
